# MT-P0 设计稿 · 汇总与 Orchestrator 裁决

> 日期：2026-07-22 ｜ 阶段：多租户改造 MT-P0（设计稿，无代码改动）
> 本文是 MT-P0 的总入口：交付物索引、跨文档一致性裁决（J1–J12）、对 docs/17/20/21 的勘误回填记录、风险登记新增。
> 五份设计稿由 5 个子代理并行产出（均实读代码、论断带 文件:行号），Orchestrator 对上报的冲突项逐一核码裁决。

---

## 1. 交付物索引

| 文档 | 内容 | 规模 |
|---|---|---|
| [docs/21-MT-P0-schema变更清单.md](21-MT-P0-schema变更清单.md) | 44 业务表逐表分类终表；tenants/users/tenant_settings/tenant_news_pushed/agent_config_overrides 五张新表 DDL；v120 加列/v121 重建 7 张/v122 对账三步迁移设计；DB 外落盘隔离；备份修复（R5） | 614 行 |
| [docs/22-MT-P0-查询审计清单-API路由层.md](22-MT-P0-查询审计清单-API路由层.md) | 26 个 protected router + auth.py，201 处 db.query 逐条标注 a/b/c/d 四类；缓存加租户清单；auth.py 改造清单 A1–A10；每文件工作量分级 | — |
| [docs/23-MT-P0-查询审计清单-运行时调度与Agent链.md](23-MT-P0-查询审计清单-运行时调度与Agent链.md) | 穿透链 22 点位（P1–P22）；T17 单 job 租户扇出结构；通知去重 scope 字符串兼容设计；事件门 state v2 分层契约 + 幂等迁移脚本；解析链重写 | — |
| [docs/24-MT-P0-查询审计清单-策略与子系统.md](24-MT-P0-查询审计清单-策略与子系统.md) | 策略层约 100 处查询逐文件属主判定（T19 落地）；entry_candidates 拆语义；tradingagents 6 文件逐点；凭证访问点 17 处；cost_tracker 配额接入点 3 处 | — |
| [docs/25-MT-P0-身份穿透设计.md](25-MT-P0-身份穿透设计.md) | JWT claims（含 pwd_at 吊销）；bcrypt 透明重哈希；contextvar + do_orm_execute fail-closed 三段规则；default-deny helper；前端 localStorage 切换清空；/reports HMAC 签名 URL；机制点验收测试设计 | 约 27KB |

## 2. Orchestrator 裁决（子代理上报冲突项，已逐条核码）

| # | 议题 | 裁决 | 依据 |
|---|---|---|---|
| J1 | portfolio_risk_snapshots 属主 | **改判市场级全局，免于 v121 重建** | Orchestrator 核码：该表由 `_apply_portfolio_constraints` 对 StrategySignalRun 信号池聚合（total/active/held_signals 均指信号行，`strategy_engine.py:1010-1059`、调用点 `:1384`、读取 `:2048-2054`），**不是用户持仓组合**；docs/17:46「语义本身就错」条目前提不成立，UQ(snapshot_date,market) 本就正确。docs/21 已修订（#35 行、v121 重建 8→7 张） |
| J2 | strategy_signal_runs 是否加 tenant_id | **加**：market_scan/mixed 源行=tenant_id=0 哨兵，watchlist 源行=候选 tenant；strategy_outcomes 由父派生 | 信号行拷贝候选行的 signal/reason/evidence（`strategy_engine.py:1364-1366`），watchlist 源候选为租户行 → 信号行携带租户 AI 文本；TA 信号去重键 (snapshot,symbol,market,strategy,source_candidate_id) 跨租户互覆（`paper_trading_bridge.py:60-72`）。docs/21 #27/#28 已修订 |
| J3 | entry_candidates tenant_id=0 哨兵 | **采纳**（docs/21 #22、docs/24 一致）；do_orm_execute 对该表与 strategy_signal_runs 注册 `tenant_id IN (:ctx, 0)` 特例 | T19 按 candidate_source 拆语义的落地方式；特例规则已指定回 docs/25 机制点表白名单 |
| J4 | notify_throttle 是否重建 UQ | **不重建**（C8/R6 优先于 docs/20-M5）：tenant 编进 scope 字符串 `__notify__:{tenant}:{hash}`，旧行 v120 附幂等 UPDATE 回填；docs/20-M5 加勘误 | docs/23 §3 兼容设计；M5 与 R6 冲突时修订节优先（用户已确认的 R8 决策表 T17 配套） |
| J5 | agent_prediction_outcomes 属主 | **判私有（租户级）**：记录租户级 Agent（intraday_monitor 等）建议的后验，非 T19 市场级信号链 | docs/21 #20 与 docs/24 判定一致；T19 的市场级链指 strategy_* 链路 |
| J6 | T4 override 表是否含 schedule 字段 | **禁含 schedule**：调度 cadence 全实例唯一（T17 单 job 硬约束），override 只允许 enabled/ai_model_id/params 类字段 | docs/23-F2；含 schedule 会与单 job 扇出直接冲突 |
| J7 | docs/20-M5 重建清单漏 news_topic_snapshots | **勘误成立**：UQ(snapshot_date,window_days) 两租户同天必撞，已入 docs/21 v121 清单（第 6 处）；docs/20 加勘误 | docs/21 #18 |
| J8 | suggestion_pool 去重键无 tenant | **新静默串扰确认**（风险登记 #19）：跨租户互吞，去重键加 tenant，纳入 MT-P3 | docs/24-F1，`suggestion_pool.py:99-132` |
| J9 | TA 进程级 env 注 key | **确认隐患**（风险登记 #20）：`llm_adapter.py:106-108` 多租户并发互覆；MT-P3 改为按调用传 key（禁注 env），过渡期为管理员托管单 key | docs/24-F3 |
| J10 | settings.py PUT /{key} 无白名单 | **新安全发现**：`settings.py:183` 可写任意 key（含 jwt_secret），MT-P1 加 key 白名单+管理员限定；change-password 补旧密码校验 | docs/22 §8 |
| J11 | 杂项采纳 | log_entries 系统日志归 tenant=0（T20 补充）；localStorage 设备级键（主题等）豁免切换清空；/reports 旧外链在 MT-P2 启用签名校验后失效、无兼容期（沿用 M10 风格）；env 凭证回退仅限管理员租户；stock_link_platform 归租户级；providers 排他 update 实为 3 处（:138,:158,:231，docs/17 漏 :158，已勘误）；discovery._cache 入缓存穷举清单 | docs/22/23/25 上报项 |
| J12 | chat.py:462 等自建 SessionLocal | **机制点硬约束**：do_orm_execute 事件必须挂**全局 Session 工厂**（而非单个 engine/session），否则自建 session 绕过自动过滤；docs/25 已吸收 | docs/22 §8-⑤ |

## 3. 勘误回填记录（本文生效时已落笔）

- **docs/21**：#27/#28/#35 三行改判（J1/J2）；v120 适用表清单 30→32 张；v121 重建 8→7 张；§9 U-1/U-5 由 J3/J5 关闭。
- **docs/17 R1 追加**（见 R1 第 8–10 条）：portfolio_risk 条目作废说明；providers 排他 update 3 处；server.py 行号勘误（resolve_ai_model 实 :1022、resolve_notify_channels 实 :1072、load_portfolio_for_agent 实 :866、log_handler 实际位于 src/web/）。
- **docs/20 追加勘误节**：M5 清单漏 news_topic_snapshots、M5 notify_throttle 项被 J4 取代、portfolio_risk_snapshots 误判更正。
- **docs/17 风险登记追加**：#19 建议池去重键无 tenant（J8）；#20 TA env 注 key 并发互覆（J9）。

## 4. MT-P0 结论

五份设计稿 + 12 项裁决构成完整施工蓝图，**无遗留阻塞性冲突**。MT-P0 通过标准（交付物齐、冲突清零、决策表 T1–T21 全部有落地载体）已满足，请用户审计。

**下一步（等用户指令）**：MT-P1 身份骨架——users/tenants 表 + JWT claims + bcrypt 透明重哈希 + /auth/me + 邀请制注册（默认关闭）+ 废除未设密码放行 + settings key 白名单 + 前端身份显示。MT-P1 上线时行为等价单用户（T12），门禁：611 基线零修改全绿 + 机制点验收测试（docs/25 §7）。
