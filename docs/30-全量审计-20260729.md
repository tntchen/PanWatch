# 30 · 全量审计报告（2026-07-29）

> 性质：覆盖全项目（代码 + git 提交史 + docs/01–29）的端到端审计，含实证验证。
> 后续行动：三项决策（D-审1/2/3）已由所有者确认并施工完毕，执行记录见 §7。

## 1. 范围与方法

- 扫描全部 git 提交史（重点：fork 后二开 30+ 提交）、docs/ 29 份文档、未提交改动。
- 通读：`server.py`、`src/web/`（app/database/tenant_context/auth/log_handler/migrations）、`src/core/`（price_alert_engine、paper_trading_engine、ai_client、notifier、scheduler）、`src/agents/morning_brief.py`、`packages/marketdata/`（engine、defaults、tushare vendor、client、ports）。
- 实证：主测试套件、marketdata 套件、tsc/vite 构建；对疑似漏洞做 ASGI 级实测（绕过 httpx 点段规范化）。

## 2. 近期变更核对（无误）

| 变更 | 审计结论 |
|---|---|
| MT-P1~P5 多租户 | 机制完备：do_orm_execute 挂全局 SessionLocal、before_flush INSERT 归属、哨兵表 IN(ctx,0)、中间件每请求清 contextvar、调度单 job 租户扇出 |
| Tushare 回归 | 惰性导入、token 双源、priority=10 默认禁用、requirements 已加 `tushare>=1.4.29,<2`，符合 D29-1 |
| DBLogHandler 修复（0.4.2） | 正确：emit 纯内存 O(1)，Timer 线程锁外写库，失败 fail-soft 不背压 |
| 引擎 fetch-then-write（0.4.3） | 正确：price_alert 命中先 commit 释放写锁再发通知；模拟盘循环前批量预取、循环体零查询 |
| R2 数据源韧性 | 主体落地（熔断门 + (datatype,vendor,market) 隔离 + 交易简报抑制），两处偏差见 §4 并已修复 |

## 3. P0 发现：SPA 静态路由路径穿越（已实测确认，已修复）

`server.py` `serve_spa` 旧实现 `os.path.join(static_dir, path)` + `isfile` 直接放行。
实测：`GET /../data/panwatch.db`（原始 ASGI scope，等价 curl --path-as-is）返回 200 + `SQLite format 3` 文件头。

- 无需认证；服务监听 0.0.0.0:8000，LAN 内任何人可下载整库（AI keys、通知渠道 token、bcrypt 哈希、jwt_secret——后者可伪造 JWT 与报告签名 URL）。
- 修复：realpath 归一化 + 静态根前缀校验（复用 `_serve_report_file` 范式），越界一律回退 index.html。

## 4. P1 发现：R2 与 docs/29 的两处偏差（已修复）

1. 熔断阈值硬编码：`defaults.py` 旧版 3 次/300 秒写死在 `_Metrics.record`，违背 docs/29「阈值和时长不写死在业务逻辑中」。
2. 错误分类未落地：docs/29 要求 token 缺失/鉴权失败单列配置异常；实际 tushare vendor 把配置与传输异常全吞成 `[]`，Engine 统一记 `empty`——分类失真且该源永远无法熔断。

## 5. P2 观察项（不阻断，登记备查）

- `src/core/ai_client.py:19` proxy 参数为 no-op（实际靠 env 代理生效），代码有误导性。
- 租户过滤对 relationship 懒加载不生效（已知设计取舍，MT-P5 已验收），残余风险登记。
- `_check_exits` 信号预取按市场全量拉 active 行再内存去重，信号表增长后需关注性能。
- CORS `allow_origins=["*"]` + `allow_credentials=True` 语义矛盾，建议收窄。
- `.DS_Store` 被 git 跟踪；`plan.md` 为历史读懂计划，可归档。
- 已验证无问题：`/api/health` 公开与 `/api/health/selfcheck` 鉴权无冲突；SQL f-string 全为内部标识符；无 shell=True/eval/exec。

## 6. 审计时验证基线

```
tests/                 829 passed   packages/marketdata  217 passed
frontend tsc -b  ✅    vite build  ✅
```

## 7. 修复执行记录（2026-07-29，待所有者审计）

### P0（D-审1）

1. `server.py` `serve_spa`：realpath 归一化 + `_static_root` 前缀校验，越界回退 index.html。
2. 新增 `tests/test_spa_static.py` 4 例：数据库/源码穿越阻断、嵌套穿越阻断、正常静态文件不受影响（原始 ASGI scope 构造未归一化路径）。

### R2 偏差（D-审2）

3. `packages/marketdata/src/marketdata/errors.py`：新增 `ConfigError(VendorError)`——配置异常（token 缺失/鉴权/依赖未安装）。
4. `defaults.py`：`InMemoryMetricsSink(circuit_failure_threshold=, circuit_cooldown_sec=)` 构造参数 > 环境变量 `MARKETDATA_CIRCUIT_FAILURE_THRESHOLD` / `MARKETDATA_CIRCUIT_COOLDOWN_SEC` > 默认 3 次/300 秒；`_Metrics` 增加 `last_error_class` / `consecutive_config_failures`，config 异常单列计数不熔断、empty 不熔断、transport 计入熔断；snapshot 透出新字段。
5. `ports.py`：MetricsSink protocol record 增加 `error_class` 参数（默认 ""，旧实现可忽略）。
6. `engine.py` / `client.py`：异常按 `isinstance(e, ConfigError)` 归类 config/transport，空数据归类 empty，随 record 下传。
7. `vendors/tushare.py`：缺包/缺 token 改抛 `ConfigError`；`pro.daily` 传输异常不再吞成 `[]`，原样上抛归类 transport（此前永远无法熔断）。
8. 测试：`test_engine.py` +4 例（config 不熔断且单列、transport 熔断、构造参数阈值、env 阈值）；`test_tushare_kline.py` 3 例改为断言 ConfigError/异常上抛。

### 文档（D-审3）

9. 本报告固化为 docs/30；docs/16 状态卡刷新；docs/29 §R2 执行记录追加纠偏条目。

### 修复后基线

```
tests/                 833 passed（+4 SPA 回归）
packages/marketdata    221 passed（+4 熔断/分类回归，另 3 例 tushare 语义更新）
```

### 刻意边界

- 其余 vendor 未逐一改抛 ConfigError：当前仅 tushare 有配置态（token/cookie 类源如雪球 cookies 缺失按业务返回空，不算异常）；后续新增凭证型 vendor 时应复用 ConfigError。
- 熔断参数仅内存态，重启清空（与 R2 原决策一致，先观察实际阈值）。
- 前端/数据源页未新增 error_class 展示：snapshot 已透出字段，UI 展示列入后续小项。
