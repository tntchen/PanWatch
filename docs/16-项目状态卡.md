# PanWatch 二开 · 项目状态卡

> 用途：新会话开场把本文交给 Kimi，即可恢复全部工作上下文。
> 更新时机：每个 Phase 完成 / 用户做关键决策后。**最后更新：2026-07-29（全量审计完成：P0 路径穿越 + R2 两处偏差已修复，待所有者审计后提交）**

## 1. 项目身份

- 工作区：`/Users/jonah/Documents/PanWatch`，fork 自开源 PanWatch（FastAPI + React/TS/Vite/Tailwind，SQLite）。
- 二开目标：① 持仓页支持加仓/减仓/流水；② 把 Kimi chat 的「东芯股份(688110)分析+跟踪」全流程融合进项目的 Agent 体系。
- 用户的东芯跟踪素材：`docs/finance_project/`（分析报告、实施方案 v3.1、定时监控配置、验证备忘录）。

## 2. 协作约定（用户明确要求，务必遵守）

1. **阶段门禁制**：计划文档先行，用户确认决策点后才能施工；每 Phase 完成提交变更清单供用户反复审计。这是中等复杂度项目，用户要求"反复确认"，后续开发都要记住。
2. **结论性文档存 `docs/`**：计划 `docs/12`（唯一事实来源，当前 v1.6）、评审 `docs/13`、验收 `docs/14`、数据核查 `docs/15-跟踪数据能力核查`。
3. 角色分工惯例：用户确认决策 → Orchestrator 派子代理并行施工（文件范围严格划分）→ Orchestrator 亲自集成/构建/冒烟 → 更新 docs/12 §8 执行记录 → 提交审计报告。

## 3. 已完成阶段（勿重做）

| 阶段 | 内容 | 状态 |
|---|---|---|
| P1 | 持仓交易化：position_trades 表、`src/core/position_trading.py`（Decimal 成本引擎）、trades API、前端加减仓/流水组件 | ✅ 已验收 |
| P2a | 方案档案：stock_playbooks 表、`src/core/playbook.py`、契约 A/B 四端点、前端方案面板。**东芯 v3.1 已导入真实库（playbook_id=1, stock_id=6）** | ✅ 已验收 |
| P2b | 触发器扩展：turnover_rate / capital_flow / consecutive_close 三新条件、CN 门控、rules.playbook_id（v119 迁移） | ✅ 已验收 |
| P3a | 注入层：context_builder payload 加 `"playbook"` 键；PositionInfo 加 `trades`/`trades_text` | ✅ 已审计（docs/30） |
| P3b | daily_report 改版：档案章节（触发器逐项/流水精确盈亏/次日预案/日历/流水纪律点评）+ 龙虎榜章节；无档案零漂移（sha256 快照断言） | ✅ 已审计（docs/30） |
| P3c | 盘中接入：事件门动态解析方案价位（穿越即事件，30 分钟冷却）；盘中 prompt 注入方案摘要 | ✅ 已审计（docs/30） |
| P3d | 新 Agent：早盘简报 `0 10 * * 1-5`（三选一）、尾盘简报 `45 14 * * 1-5`（五选一+数量价格区间）；档案门控+契约校验不过不推送；daily_report seed 改 `0 18` | ✅ 已审计（docs/30） |
| P3e | marketdata 包：科创50、margin_series、board_quote/board_klines、Bar.turnover | ✅ 已审计（docs/30） |
| P4 | 报告归档：47 页东芯报告导入 analysis_history（幂等）；docx 落位 `data/reports/` + `/reports/{filename}` 下载路由 | ✅ 已审计（docs/30） |
| R1 | Tushare 镜像必装依赖（requirements.txt `tushare>=1.4.29,<2`） | ✅ 已确认（docs/29） |
| R2 | 数据源健康/熔断/降级 + 交易简报 data_unavailable 抑制 | ✅ 已确认（docs/29）；两处偏差已纠偏（docs/30 §7） |
| 审计 | 全量审计（docs/30）：**P0 SPA 路径穿越已修复**（serve_spa realpath 校验 + 4 例回归）；R2 偏差修复（熔断阈值可配置 + ConfigError 错误分类 + tushare 异常语义） | ✅ 已发布 0.4.5 |

**测试基线：主套件 833 passed + marketdata 包 221 passed、0 xfail**；前端门禁 `./node_modules/.bin/tsc --noEmit && ./node_modules/.bin/vite build`（2026-07-29 实测全过）。
**git 已提交至 `374986d` 并推送远端**（P0 修复 `5922b5a`、R2 纠偏 `bd10364`、文档 `374986d`）。
**0.4.5 已发布并部署 NAS（2026-07-29）**：tag 0.4.5（push 未触发流水线，走 workflow_dispatch run 30386935436 ≈21min success）→ `chentnt/panwatch:0.4.5` + `:latest`；NAS compose 已备份（`.bak-0.4.4-20260729`）并切到 0.4.5，容器 healthy、`/app/VERSION`=0.4.5、四套调度器正常启动、P0 穿越实测返回 index.html（修复线上生效）。回滚点：compose 改回 `:0.4.4` + `up -d`。

## 4. 真实库关键状态（`data/panwatch.db`）

- 账户：仅「默认账户」(id=1)（冒烟账户已删）。东芯持仓 position id=1：2150 股 @112.572，5 笔流水。
- 东芯方案档案 playbook id=1, stock_id=6, v1, is_active=1；trigger_hints 规则名：右侧批触发区/做T卖出区/防线98/换手率<4%/主力净流入/冻结信号。
- stock_agents 绑定：东芯(6) → morning_brief / tail_brief / daily_report / intraday_monitor（P3 冒烟时代用户绑定）。
- Agent 种子：morning_brief/tail_brief enabled=0；daily_report enabled=1 但 schedule 仍是 `30 15 * * 1-5`（seed 不覆盖已有配置）。
- **本机无任何 AI 凭据**（ai_services 空、无 .env、无环境变量）——Agent 实盘调用会报 Missing credentials。

## 5. 待用户处理事项（阻塞实盘运行）

1. UI 配置 AI 服务（模型+密钥）；2. UI 启用 morning_brief / tail_brief；3. UI 把 daily_report 改 `0 18 * * 1-5`；4. UI 配置东芯 7 条正式触发器并关联 playbook_id=1；5. 审计本轮 P0+R2 纠偏变更（docs/30 §7）后在 NAS 发布含 P0 修复的新版本——**当前线上 0.4.3 存在已修复的路径穿越漏洞，建议优先发布**。

## 6. 关键坑（血泪教训）

- **`src/web/database.py:14` DB_PATH 硬编码 `./data/panwatch.db`，web 层不认 DATA_DIR**——本地冒烟会写真实库；测试一律内存库/TestClient。
- **前端构建禁止 corepack pnpm**（污染 package.json / pnpm-workspace.yaml）；用 `cd frontend && ./node_modules/.bin/tsc -b && ./node_modules/.bin/vite build`。
- 服务重启：`pkill -f "python server.py"` 后 `(.venv/bin/python server.py > /tmp/p3_live.log 2>&1 &)`；前端产物同步 `rm -rf static && cp -r frontend/dist static`。
- `/api/` 前缀的响应会被 ResponseWrapper 缓冲——大文件下载路由必须避开 /api/ 前缀。
- prompt 零漂移保障模式：条件段标记 + sha256 快照断言（daily_report/intraday 同构）。

## 7. 遗留技术项

龙虎榜 18:00 出数实测（人工签字项）；P3e 新数据源真实响应校准；事件门"方案参数覆盖阈值"待档案 schema 结构化；test_brief_agents 并发用例偶发失败（观察中）；D11 平行对照期 10 个交易日自用户启用 Agent 后开始。

## 8. 决策记录

D1–D11 全部确认（明细见 docs/12 §5）；D10 多股票推广后置；D11 平行对照同意。**品牌改名：2026-07-22 已完成（docs/18），中文「盯盯」+ 英文保留 PanWatch，技术标识符未动**。**多租户改造：T1–T21 全部确认（docs/17 v1.1 R8）；MT-P0 设计稿完成（docs/21–26）；✅ MT-P1 身份骨架已完成（docs/17 末尾执行记录，2026-07-22）：users/tenants 表+JWT+bcrypt 透明重哈希+邀请制+机制点（单租户直通）+前端身份/用户管理页，测试基线升 657 passed，待用户审计；✅ MT-P2 数据层隔离已完成（v120/121/122 已落真实库）；✅ MT-P3 运行时穿透已完成（docs/17 末尾执行记录）：调度单 job 租户扇出、事件门状态分层、通知节流租户化、TA env 注 key 改造、v123 信号表 UQ 补 tenant_id，测试基线升 712 passed，git 已提交 f6856ef（MT-P1/P2），MT-P3 已提交 89a7aeb（含建议池去重 datetime 修复——已裁决修复并补回归测试）；✅ MT-P4 前端与配置属主化已完成（docs/17 末尾执行记录）：凭证 API 掩码+可见集、AI 服务两态 UI、/reports HMAC 签名 URL、权限 UI 分化（数据源/策略写操作仅管理员）、localStorage 用户级隔离、F6 env 回退收紧，测试基线升 740 passed，MT-P4 已提交 b8f265b；✅ MT-P5 验收已完成（docs/17 末尾执行记录）：78 例跨租户泄漏/缓存/prompt 内容级断言套件 + 验收发现的 7 项真缺陷全部修复闭环（INSERT 归属 before_flush 钩子、settings 写面租户化、count 盲区、templates 权限）+ docs/01/02/03/11 回写，测试基线升 824 passed 零 xfail，改动未提交；MT-P5 已提交并推送 5fce098；多租户六期（P0→P5）全部闭环；✅ Tushare K线数据源回归完成（docs/19，D1–D5 全部确认：vendor+注册+种子 priority=10 默认禁用+D5 config 掩码+12 例 vendor 单测），真实取数待装 tushare 包配 token 手动验证，改动未提交**。另有 docs/19-tushare 数据源回归计划（D1–D4 待确认）。

## 9. 新会话快速恢复指令

```
读 docs/16-项目状态卡.md 恢复上下文 → 多租户相关按 docs/17 v1.1 + docs/20；
测试基线 主 833 + marketdata 221 passed；git 已推送至 99b37a7；待用户处理事项见 §5；
全量审计结论与本轮修复清单见 docs/30；阶段门禁不变：所有者确认决策点前不动代码。
```
